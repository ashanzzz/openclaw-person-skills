# 2026-03-04

## 记录
- 用户反馈：此前 `openclaw.json` 出现 unknown keys 报错（`agents.defaults.subagents.allow`、`agents.list[*].fallback`）。
- 用户新增项目：`wordmonitor`，希望后续提醒其部署试用。
- 用户请求：希望索取 `secure/tokens/tokens.env`（包含密钥，需谨慎处理）。
- 职业规划（女朋友/应届，用户补充，2026-03-04）：通勤起点天津东丽湖；地铁1小时/开车50分钟可接受；可轻微加班；可按3年周期准备；行业偏好汽车/制造/医疗/教育等；公司种子：大众、麦格纳、汉特曼、约翰迪尔、卡特彼勒、博格华纳、霍尼韦尔等；尽量全资外企；到岗时间随时；薪资中等水平即可；编号11=采购/供应链量化成果（待补充）；编号13=尝试投递空客（Airbus）但不太好。

## 配置专家门控规则（用户新增）
- openclaw-config-expert 修改任何配置前必须先学习/核验官方文档（本地 /app/docs + 在线 docs.openclaw.ai），禁止猜配置键。
- 每次变更后必须跑 openclaw doctor + openclaw status --all，防止 unknown keys 导致 Config invalid。

## Skill → Agent 转换（用户要求，2026-03-04）
- 已创建 agent：`openclaw-config-expert`（workspace: /root/.openclaw/workspace-openclaw-config-expert）。
- 已删除 skill：`/root/.openclaw/workspace/skills/openclaw-config-expert`。
- 已备份被删除的 skill 到：`secure/backups/skill_openclaw-config-expert_20260304.tar.gz`。

## 按用户要求删除专家类 skills（2026-03-04）
- 已删除 workspace skills 目录下 8 个 skill：career-development-expert、ceo-mode、eta-progress-guard、invoice-pdf-extractor、job-hunt-orchestrator、self-improving-agent、self-improving-agent.bak、sinopec-invoice-helper。
- 已核对 /app/skills：不存在这些同名目录，因此无需删除 /app/skills 侧内容。

## Agent 合并（去重）
- 已将 `management-agent` 与 `factory-knowledge-agent` 从 openclaw.json agents.list 移除，统一由 `factory-operations-expert` 承担管理+工厂+商业。
- 保留 `project-manager-agent` 作为项目管理专用（可按需再合并）。

## 回复署名规则（用户新增）
- 每次回复开头或结尾需标注：本次回复由 Zero 或 Zero+若干Agent 联合提供；并注明A2A沟通次数（约几轮）。

## A2A回写规则（用户确认）
- 规则：若代理A与代理B私聊（未经过Zero发起），则必须在对用户输出前回写Zero一条摘要；否则Zero不将其计入“联合提供”。

## A2A连通性修复（系统性）
- 根因：健康测试要求“报当前时间”会触发 agent 调用 `session_status` 等工具，导致回复延迟并引发 sessions_send 超时。
- 修复：已在所有 agent 的 `AGENTS.md` 增加 `A2A Ping Protocol`：ping 时禁止调用工具，统一 time=unknown 单行快速回。

## 清理旧 workspace-* 目录（用户要求）
- 已删除：/root/.openclaw/workspace-*（包括 china-career/erp/news/unraid/programmer/project-manager/factory-ops/openclaw-config 以及 workspace-main/workspace-zero 等），当前仅保留主工作区 /root/.openclaw/workspace。

## UNRAID_LOGGING_NOTES_v1（用户确认）
- 已固化：`secure/unraid/UNRAID_LOGGING_NOTES_v1.md`
- 要点：双面日志=stdout/stderr持续输出 + `exec > >(tee -a "$LOG_FILE") 2>&1`；避免吞输出；`stdbuf` 解决缓冲；日志目录优先 `/mnt/cache` 或 `/var/log`；可用 `logger` 入 syslog；User Scripts 关闭后台/静默并用 View Log/Tail。

## 用户关注重点（新增）
- 用户明确更看重 RAG/知识库与记忆能力；多 agent 配置的意义在于并行与知识沉淀，而非 agent 数量本身。

## Agent 生命周期（新增规则）
- 用户要求：长时间不活跃的专家（约 2 个月）就删除，以保持体系精简。

## A2A 循环防护（用户规则）
- 单个 job 允许多轮确认，但 A2A 往返上限约 20 条（用户要求从30收紧到20）；Zero 判断没问题必须 DONE 并停止继续回复该 agent（除非新 job_id）。
- 用户新增偏好：Zero↔Agent 往返**尽量提前结束**；软上限 ≤20，能提前 DONE 就提前。

## 执行优先级（用户明确，2026-03-04）
- 最高优先：把任务做完（达成目标/可交付）。
- 次要目标：在不牺牲完成度与正确性的前提下，尽量减少往返沟通成本。
- 专家调度规则（用户确认，2026-03-04）：用户明确只要“一个结果/结论”时，Zero 应按需调度专家并门控交付；若 Zero 自己可直接完成且满足需求，可不经过专家以减少往返。

## 可靠性偏好（用户新增，2026-03-04）
- 若出现 `Concurrency limit exceeded for account, please retry later`：Zero 需要等待约 60 秒后自动重试，而不是立刻让用户手动再发一次。

## 自省/修正（2026-03-04）
- grep 多关键字 OR 搜索：使用 `grep -E`/`grep -RE`（或 `rg`）才能让 `|` 表示 OR；否则会按字面匹配整串。
- 调试阶段不要盲目 `2>/dev/null` 吞错误：先让错误输出暴露以确认路径/权限问题，再决定是否静默。

## 职业规划/招聘 Agent 基础信息落地（2026-03-04）
- 已完善 `china-career-agent` 工作区的基础 Bootstrap 文件：AGENTS.md / IDENTITY.md / USER.md / SOUL.md / TOOLS.md
- 已在 `workspace-china-career-agent` 添加 `secure` 软链，便于直接读取 `secure/career-db/*`。
- 已更新 `secure/career-db/profile_user.md`，补齐岗位检索硬约束与偏好。
