# Exception Playbook

## 浏览器不可用
- 退化到 `web_fetch` + 官方 ATS 查询参数页。
- 全部结果标记 `verify_needed`，并注明“未完成浏览器详情核验”。

## 登录墙/验证码
- 暂停自动化并请求用户协助一次登录。
- 登录后立即继续抓取，不重复请求。

## 字段缺失
- 先保留原文片段，再二次抓取。
- 二次仍缺失：标 `verify_needed`，禁止宣称“完成”。

## 站点反爬
- 降低请求频率，切换官方镜像/地区站点。
- 严禁伪造数据填充缺失字段。
